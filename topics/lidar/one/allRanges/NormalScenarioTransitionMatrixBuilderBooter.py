import numpy as np

from mMath.statistic.Distance import Distance

leadership = "leader"
sensorName = "lidars"
basePathToProject = "/home/donkarlo/Dropbox/projs/research/data/self-aware-drones/ctumrs/two-drones/"
def loadScenarioLowDimLidarData(leadership,scenarioName)->np.ndarray:
    pathToScenrio = basePathToProject + "{}-scenario".format(scenarioName)


########## Build transition matrix from normal scenario
# load lidar normal data from relevant robot
secnarionName = "normal"

twoLidarsTimeRangesObss.pk
test
# reduce the dim of data using relevant encoder
# cluster data
# build one alphabet transition matrix

secnarionName = "follow"
########## load follow scenraio leader data
# reduce the dimension using encoder
# detect abnormality
